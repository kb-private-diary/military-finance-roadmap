<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.member.mapper.MilitaryTypeMapper">

    <select id="findMilitaryTypeList" resultType="MilitaryTypeVO">
        SELECT type_id, type_name, regular_vacation_days,
               created_date, created_nm, modified_date, modified_nm, del_yn
        FROM military_types
        WHERE del_yn = 'N'
        ORDER BY type_id
    </select>

    <select id="findMilitaryType" resultType="MilitaryTypeVO">
        SELECT type_id, type_name, regular_vacation_days,
               created_date, created_nm, modified_date, modified_nm, del_yn
        FROM military_types
        WHERE type_id = #{typeId}
          AND del_yn = 'N'
    </select>

</mapper>
