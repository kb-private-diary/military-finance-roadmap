<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.roadmap.mapper.RoadmapMapper">

    <select
            id="findRoadmapList"
            parameterType="map"
            resultType="org.scoula.roadmap.dto.RoadmapListResponseDTO">

        SELECT
        tg.goal_id AS goalId,
        ub.bookmark_id AS bookmarkId,
        1 AS categoryId,
        tg.title AS title,
        DATE_FORMAT(tg.start_date, '%Y.%m.%d') AS targetDate,
        DATE_FORMAT(tg.created_date, '%Y.%m.%d') AS createdDate,
        tg.destination AS detail,
        CASE
        WHEN ub.bookmark_id IS NOT NULL THEN TRUE
        ELSE FALSE
        END AS bookmarked
        FROM travel_goal tg

        LEFT JOIN user_bookmark ub
        ON ub.user_id = tg.user_id
        AND ub.category_id = 1
        AND ub.goal_id = tg.goal_id
        AND ub.del_yn = 'N'

        WHERE tg.user_id = #{userId}
        AND tg.status = 'CONFIRMED'
        AND tg.del_yn = 'N'
        AND (#{category} = 'all' OR #{category} = 'travel')


        UNION ALL


        SELECT
        jg.goal_id AS goalId,
        ub.bookmark_id AS bookmarkId,
        2 AS categoryId,

        CASE
        WHEN jg.goal_type IN ('J01', 'J02')
        THEN jc2.category_name

        WHEN jg.goal_type = 'J03'
        THEN CONCAT(
        REPLACE(jtu.univ_name, '대학교', '대'),
        ' · ',
        jtmc.major_name
        )

        ELSE '진로 목표'
        END AS title,

        REPLACE(jg.expected_date, '-', '.') AS targetDate,

        DATE_FORMAT(jg.created_date, '%Y.%m.%d') AS createdDate,

        CASE
        WHEN jg.goal_type = 'J01'
        THEN CONCAT('취업 ', jc1.category_name)

        WHEN jg.goal_type = 'J02'
        THEN CONCAT('공무원 ', jc1.category_name)

        WHEN jg.goal_type = 'J03'
        THEN '편입'

        ELSE NULL
        END AS detail,

        CASE
        WHEN ub.bookmark_id IS NOT NULL THEN TRUE
        ELSE FALSE
        END AS bookmarked

        FROM job_goal jg

        LEFT JOIN job_category jc2
        ON jg.category_id = jc2.category_id
        AND jc2.del_yn = 'N'

        LEFT JOIN job_category jc1
        ON jc2.parent_id = jc1.category_id
        AND jc1.del_yn = 'N'

        LEFT JOIN job_transfer_university jtu
        ON jg.univ_id = jtu.univ_id
        AND jtu.del_yn = 'N'

        LEFT JOIN job_transfer_major jtm
        ON jg.major_id = jtm.major_id
        AND jtm.del_yn = 'N'

        LEFT JOIN job_transfer_major_category jtmc
        ON jtm.major_code = jtmc.major_code
        AND jtmc.del_yn = 'N'

        LEFT JOIN user_bookmark ub
        ON ub.user_id = jg.user_id
        AND ub.category_id = 2
        AND ub.goal_id = jg.goal_id
        AND ub.del_yn = 'N'

        WHERE jg.user_id = #{userId}
        AND jg.status = 'CONFIRMED'
        AND jg.del_yn = 'N'
        AND (#{category} = 'all' OR #{category} = 'job')


        UNION ALL


        SELECT
        cg.goal_id AS goalId,
        ub.bookmark_id AS bookmarkId,
        3 AS categoryId,

        COALESCE(
        CONCAT(
        cm.model_name,
        CASE
        WHEN cg.selected_year IS NOT NULL
        THEN CONCAT(' (', cg.selected_year, ')')
        ELSE ''
        END
        ),
        '자동차 구매 목표'
        ) AS title,

        DATE_FORMAT(cg.target_date, '%Y.%m.%d') AS targetDate,
        DATE_FORMAT(cg.created_date, '%Y.%m.%d') AS createdDate,
        CASE cg.car_type_code
        WHEN 1 THEN '경차'
        WHEN 2 THEN '준중형'
        WHEN 3 THEN 'SUV'
        ELSE NULL
        END AS detail,

        CASE
        WHEN ub.bookmark_id IS NOT NULL THEN TRUE
        ELSE FALSE
        END AS bookmarked

        FROM car_goal cg

        LEFT JOIN car_model cm
        ON cg.selected_model_id = cm.model_id
        AND cm.del_yn = 'N'

        LEFT JOIN user_bookmark ub
        ON ub.user_id = cg.user_id
        AND ub.category_id = 3
        AND ub.goal_id = cg.goal_id
        AND ub.del_yn = 'N'

        WHERE cg.user_id = #{userId}
        AND cg.status = 'CONFIRMED'
        AND cg.del_yn = 'N'
        AND (#{category} = 'all' OR #{category} = 'car')


        UNION ALL


        SELECT
        rg.goal_id AS goalId,
        ub.bookmark_id AS bookmarkId,
        4 AS categoryId,
        COALESCE(
        NULLIF(rl.building_name, ''),
        NULLIF(CONCAT_WS(' ', rc.umd_name, rl.estate_type), ''),
        rg.title,
        '자취 목표'
        ) AS title,
        NULL AS targetDate,
        DATE_FORMAT(rg.created_date, '%Y.%m.%d') AS createdDate,
        CONCAT_WS(' ', rc.sido_name, rc.sigungu_name, rc.umd_name) AS detail,

        CASE
        WHEN ub.bookmark_id IS NOT NULL THEN TRUE
        ELSE FALSE
        END AS bookmarked

        FROM rent_goal rg

        LEFT JOIN rent_listing rl
        ON rl.listing_id = rg.confirmed_listing_id
        AND rl.del_yn = 'N'

        LEFT JOIN region_code rc
        ON rc.region_code = rl.region_code
        AND rc.del_yn = 'N'

        LEFT JOIN user_bookmark ub
        ON ub.user_id = rg.user_id
        AND ub.category_id = 4
        AND ub.goal_id = rg.goal_id
        AND ub.del_yn = 'N'

        WHERE rg.user_id = #{userId}
        AND rg.status = 'CONFIRMED'
        AND rg.confirmed_listing_id IS NOT NULL
        AND rg.del_yn = 'N'
        AND (#{category} = 'all' OR #{category} = 'rent')

        ORDER BY createdDate DESC

    </select>

    <!-- 로드맵 목표 soft delete -->
    <update id="deleteRoadmapGoal">
        <choose>
            <!-- 여행 -->
            <when test="categoryId == 1">
                UPDATE travel_goal
            </when>

            <!-- 진로 -->
            <when test="categoryId == 2">
                UPDATE job_goal
            </when>

            <!-- 자동차 -->
            <when test="categoryId == 3">
                UPDATE car_goal
            </when>

            <!-- 자취 -->
            <when test="categoryId == 4">
                UPDATE rent_goal
            </when>
        </choose>

        SET
        del_yn = 'Y',
        modified_date = NOW(),
        modified_nm = #{modifiedNm}
        WHERE goal_id = #{goalId}
        AND user_id = #{userId}
        AND del_yn = 'N'
    </update>

    <!-- 삭제한 로드맵의 관심등록 soft delete -->
    <update id="deleteBookmarkByGoal">
        UPDATE user_bookmark
        SET
        del_yn = 'Y',
        modified_date = NOW(),
        modified_nm = #{modifiedNm}
        WHERE user_id = #{userId}
        AND category_id = #{categoryId}
        AND goal_id = #{goalId}
        AND del_yn = 'N'
    </update>

</mapper>