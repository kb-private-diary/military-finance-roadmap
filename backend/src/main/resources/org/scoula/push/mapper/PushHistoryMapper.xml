<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.push.mapper.PushHistoryMapper">

    <!-- 발송 성공/실패 상관없이 매 시도마다 한 행씩 남긴다 -->
    <insert id="insert" parameterType="org.scoula.push.domain.PushHistoryVO"
            useGeneratedKeys="true" keyProperty="historyId">
        INSERT INTO push_history (
            user_id, title, body, category, status, sent_at, created_date, created_nm, del_yn
        ) VALUES (
            #{userId}, #{title}, #{body}, #{category}, #{status},
            #{sentAt}, NOW(), #{createdNm}, 'N'
        )
    </insert>

    <!-- 사용자한테 보여주는 이력이라 실제로 받은(SUCCESS) 것만. FAILED는 내부 모니터링용이라 제외 -->
    <select id="findListByUserId" resultType="org.scoula.push.domain.PushHistoryVO">
        SELECT history_id, user_id, title, body, category, status, sent_at,
               created_date, created_nm, modified_date, modified_nm, del_yn
        FROM push_history
        WHERE user_id = #{userId}
          AND status = 'SUCCESS'
          AND del_yn = 'N'
        ORDER BY sent_at DESC
    </select>

</mapper>
