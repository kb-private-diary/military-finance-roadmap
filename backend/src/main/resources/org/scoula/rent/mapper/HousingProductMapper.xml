<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.rent.mapper.HousingProductMapper">

    <!-- 조회 컬럼 (mapUnderscoreToCamelCase 로 VO 자동 매핑) -->
    <sql id="productColumns">
        product_id, product_name, product_type, support_type, loan_type, provider, is_kb, region_code,
        rate_min, rate_max, rate_summary, support_amount, support_months, loan_limit,
        min_age, max_age, veteran_extend, veteran_note,
        deposit_limit, monthly_limit, area_limit, income_limit, join_condition,
        exclusive_group, base_year, apply_start, apply_end, apply_cycle,
        priority, detail, external_url, deeplink, created_at, updated_at
    </sql>

    <!--
      매물 조건 기반 금융상품 추천 (명세 3장 필터)
      - 지역: region_code IS NULL(전국) OR region_code = 매물 시도코드 2자리
              시도코드는 매물 법정동코드(10자리) 앞 2자리를 우선 쓰되, 법정동코드가 NULL(코드 매핑 실패)이면
              항상 채워지는 시군구코드(LAWD_CD, 5자리) 앞 2자리로 대체한다 (COALESCE). 그러지 않으면
              법정동 매핑이 실패한 매물(예: 부산 기장군)은 LEFT(NULL,2)=NULL 이라 지역(LOCAL) 상품이 모두 누락된다.
      - 매물조건: 한도 컬럼이 NULL 이면 제한 없음(통과), 값이 있으면 매물값이 한도를 초과할 때만 제외
      - 나이 필터 없음 (생년월일 부재)
      - ORDER BY priority (만료 상품 하단 배치는 서비스단에서 isOpen 으로 재정렬)
    -->
    <select id="findProducts" resultType="org.scoula.rent.domain.HousingProductVO">
        SELECT <include refid="productColumns"/>
          FROM housing_product
         WHERE (region_code IS NULL OR region_code = LEFT(COALESCE(#{regionCode}, #{sigunguCode}), 2))
           AND (deposit_limit IS NULL OR #{deposit} &lt;= deposit_limit)
           AND (monthly_limit IS NULL OR #{monthlyRent} &lt;= monthly_limit)
           AND (area_limit    IS NULL OR #{areaSqm} IS NULL OR #{areaSqm} &lt;= area_limit)
         ORDER BY priority
    </select>

    <!-- 사용자 복무기간(입대일·전역예정일) - 병역 혜택 안내용 -->
    <select id="findServicePeriodByUserId" resultType="org.scoula.rent.domain.MilitaryServiceVO">
        SELECT enlist_date, discharge_date
          FROM `user`
         WHERE id = #{userId}
           AND del_yn = 'N'
    </select>

    <!--
      온통청년 정책 UPSERT (연동명세 §5)
      - uk_plcy(plcy_no) 유니크키 기준 ON DUPLICATE KEY UPDATE
      - plcy_no 가 채워진 API 상품만 넣으므로 plcy_no IS NULL(KB 수동상품)은 건드리지 않는다
      - region_code 는 zip_cd 앞 2자리로 SyncService 가 채워, 기존 추천 쿼리(region_code 매칭)와 호환
      - api_synced_at 은 INSERT/UPDATE 모두 NOW()
    -->
    <insert id="upsertYouthPolicy" parameterType="org.scoula.rent.domain.HousingProductVO">
        INSERT INTO housing_product (
            product_name, product_type, support_type, loan_type, provider, is_kb, region_code,
            support_amount, support_months, loan_limit,
            min_age, max_age, veteran_extend, veteran_note,
            deposit_limit, area_limit, income_limit, join_condition,
            exclusive_group, base_year, apply_start, apply_end, apply_cycle,
            priority, detail, external_url,
            plcy_no, zip_cd, api_synced_at
        ) VALUES (
            #{productName}, #{productType}, #{supportType}, #{loanType}, #{provider},
            'N', #{regionCode},
            #{supportAmount}, #{supportMonths}, #{loanLimit},
            #{minAge}, #{maxAge}, #{veteranExtend}, #{veteranNote},
            #{depositLimit}, #{areaLimit}, #{incomeLimit}, #{joinCondition},
            #{exclusiveGroup}, #{baseYear}, #{applyStart}, #{applyEnd}, #{applyCycle},
            #{priority}, #{detail}, #{externalUrl},
            #{plcyNo}, #{zipCd}, NOW()
        )
        ON DUPLICATE KEY UPDATE
            product_name    = VALUES(product_name),
            product_type    = VALUES(product_type),
            support_type    = VALUES(support_type),
            loan_type       = VALUES(loan_type),
            provider        = VALUES(provider),
            region_code     = VALUES(region_code),
            support_amount  = VALUES(support_amount),
            support_months  = VALUES(support_months),
            loan_limit      = VALUES(loan_limit),
            min_age         = VALUES(min_age),
            max_age         = VALUES(max_age),
            veteran_extend  = VALUES(veteran_extend),
            veteran_note    = VALUES(veteran_note),
            deposit_limit   = VALUES(deposit_limit),
            area_limit      = VALUES(area_limit),
            income_limit    = VALUES(income_limit),
            join_condition  = VALUES(join_condition),
            exclusive_group = VALUES(exclusive_group),
            base_year       = VALUES(base_year),
            apply_start     = VALUES(apply_start),
            apply_end       = VALUES(apply_end),
            apply_cycle     = VALUES(apply_cycle),
            priority        = VALUES(priority),
            detail          = VALUES(detail),
            external_url    = VALUES(external_url),
            zip_cd          = VALUES(zip_cd),
            api_synced_at   = NOW()
    </insert>

</mapper>
