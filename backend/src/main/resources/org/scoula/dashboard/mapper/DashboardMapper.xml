<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.dashboard.mapper.DashboardMapper">

    <select id="getCurrentTotalSavings" resultType="java.lang.Long">
        SELECT COALESCE(SUM(sh.pay_amount), 0)
        FROM saving_history sh
        JOIN saving_account sa ON sh.account_id = sa.account_id
        WHERE sa.user_id = #{userId}
          AND sa.del_yn = 'N'
          AND sh.del_yn = 'N'
    </select>

    <select id="getDischargeDateByUserId" resultType="java.util.Date">
        SELECT discharge_date
        FROM user
        WHERE id = #{userId}
          AND del_yn = 'N'
    </select>

    <!-- resultType 변경 필요 : 예) org.scoula.openbanking.domain.SavingAccountVO -->
    <select id="getSavingAccountsByUserId" resultType="org.scoula.dashboard.dto.DashboardSavingAccountDTO">
        SELECT *
        FROM saving_account
        WHERE user_id = #{userId}
          AND del_yn = 'N'
    </select>
    <!-- resultType 변경 필요 : 예) org.scoula.openbanking.domain.SavingHistoryVO -->
    <select id="getSavingHistoryByAccountId" resultType="org.scoula.dashboard.dto.DashboardSavingHistoryDTO">
        SELECT *
        FROM saving_history
        WHERE account_id = #{accountId}
          AND del_yn = 'N'
        ORDER BY pay_round ASC
    </select>

</mapper>
